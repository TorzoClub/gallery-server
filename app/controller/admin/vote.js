'use strict';

module.exports = app => {
  class VoteController extends app.Controller {
    // async create(ctx) {
    // }

    // async remove(ctx) {
    // }

    // async show(ctx) {
    // }

    // async edit(ctx) {
    // }
  }

  return VoteController;
};
